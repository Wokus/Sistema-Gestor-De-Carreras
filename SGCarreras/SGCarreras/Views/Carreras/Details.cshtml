@model SGCarreras.Models.Carrera
@using System.Threading;
@using System.Security.Claims;
@using static SGCarreras.Models.Estado

@{
    ViewData["Title"] = "Detalles de la Carrera";
}

@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
    var HoraAvance = DateTime.Now;
    var PuntoControl = 0;
    // Obtener el referer (página anterior) para saber de dónde vino el usuario
    var referer = Context.Request.Headers["Referer"].ToString();
    var vieneDeCalendarios = referer.Contains("Calendarios");
    var vieneDeCarreras = referer.Contains("Carreras") || referer.Contains("Index");

    // Si no podemos determinar de dónde viene, asumimos Calendarios para usuarios normales
    var esAdministrador = User?.FindFirstValue(ClaimTypes.Role) == "Administrador";
    var accionVolver = esAdministrador || vieneDeCarreras ? "Index" : "Calendarios";
    var textoVolver = esAdministrador || vieneDeCarreras ? "Volver al Listado" : "Volver al Calendario";

    var userIdString = User?.FindFirstValue(ClaimTypes.NameIdentifier);
    int userId = 0;
    int.TryParse(userIdString, out userId);

    var email = User?.FindFirstValue(ClaimTypes.Email);
    var nombre = User?.FindFirstValue(ClaimTypes.Name);
    var rol = User?.FindFirstValue(ClaimTypes.Role);
}

<div class="container-fluid py-4">
    <div class="row">
        <div class="col-lg-8 mx-auto">
            <!-- Header de la Carrera -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1 class="display-5 fw-bold text-primary">@Model.Nombre</h1>
                    <p class="lead text-muted mb-0">Detalles completos de la carrera</p>
                </div>
                <div class="text-end">
                    @if (rol != null && rol == "Administrador")
                    {
                        <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning btn-lg">
                            <i class="fas fa-edit me-2"></i>Editar Carrera
                        </a>
                    }
                </div>
            </div>

            <!-- Tarjetas de Información Rápida -->
            <div class="row mb-5">
                <div class="col-md-3 mb-3">
                    <div class="card border-0 bg-primary text-white h-100">
                        <div class="card-body text-center">
                            <i class="fas fa-map-marker-alt fa-2x mb-3"></i>
                            <h5 class="card-title">Ubicación</h5>
                            <p class="card-text fs-6">@Model?.Ubicacion</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="card border-0 bg-success text-white h-100">
                        <div class="card-body text-center">
                            <i class="fas fa-flag-checkered fa-2x mb-3"></i>
                            <h5 class="card-title">Estado</h5>
                            <p class="card-text fs-6">@Model?.Estado</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="card border-0 bg-info text-white h-100">
                        <div class="card-body text-center">
                            <i class="fas fa-route fa-2x mb-3"></i>
                            <h5 class="card-title">Distancia</h5>
                            <p class="card-text fs-6">@Model?.KmTotales km</p>
                        </div>
                    </div>
                </div>
                <div class="col-md-3 mb-3">
                    <div class="card border-0 bg-warning text-white h-100">
                        <div class="card-body text-center">
                            <i class="fas fa-running fa-2x mb-3"></i>
                            <h5 class="card-title">Participantes</h5>
                            <p class="card-text fs-6">@(Model?.Registros?.Count ?? 0) corredores</p>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Detalles Expandidos -->
            <div class="row">
                <div class="col-12">
                    <div class="card border-0 shadow-lg">
                        <div class="card-header bg-light">
                            <h3 class="card-title mb-0">
                                <i class="fas fa-info-circle text-primary me-2"></i>
                                Información Detallada
                            </h3>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6">
                                    <dl class="row">
                                        <dt class="col-sm-4 text-muted">Nombre:</dt>
                                        <dd class="col-sm-8 fs-5 fw-bold text-dark">@Model?.Nombre</dd>

                                        <dt class="col-sm-4 text-muted">Ubicación:</dt>
                                        <dd class="col-sm-8">
                                            <i class="fas fa-map-marker-alt text-danger me-2"></i>
                                            @Model?.Ubicacion
                                        </dd>
                                    </dl>
                                </div>
                                <div class="col-md-6">
                                    <dl class="row">
                                        <dt class="col-sm-4 text-muted">Estado:</dt>
                                        <dd class="col-sm-8">
                                            @{
                                                var estadoBadgeClass = Model?.Estado.ToString() == "Activa" ? "bg-success" :
                                                Model?.Estado.ToString() == "Cancelada" ? "bg-danger" : "bg-warning";
                                            }
                                            <span class="badge @estadoBadgeClass text-white fs-6">@Model?.Estado</span>
                                        </dd>

                                        <dt class="col-sm-4 text-muted">Distancia:</dt>
                                        <dd class="col-sm-8">
                                            <i class="fas fa-route text-info me-2"></i>
                                            <span class="fw-bold">@Model?.KmTotales km</span>
                                        </dd>
                                    </dl>
                                </div>
                            </div>

                            <!-- Información Adicional (puedes expandir según tu modelo) -->
                            <div class="row mt-4">
                                <div class="col-12">
                                    <div class="border-top pt-3">
                                        <h5 class="text-primary mb-3">
                                            <i class="fas fa-calendar-alt me-2"></i>
                                            Información Adicional
                                        </h5>
                                        <div class="row">
                                            <div class="col-md-4">
                                                <small class="text-muted">Fecha de Creación:</small>
                                                <div class="fw-semibold">@Model?.Fecha.ToString("dd/MM/yyyy HH:mm")</div>
                                            </div>
                                            <div class="col-md-4">
                                                <small class="text-muted">Última Actualización:</small>
                                                <div class="fw-semibold">@DateTime.Now.ToString("dd/MM/yyyy")</div>
                                            </div>
                                            <div class="col-md-4">
                                                <small class="text-muted">Categorías:</small>
                                                <div class="fw-semibold">Abiertas</div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row mt-4">
                <div class="col-12">
                    <div class="d-flex justify-content-between align-items-center">
                        <!-- Botón Volver - Simple pero efectivo -->
                        @if (rol == "Administrador")
                        {
                            <a asp-action="Index" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-2"></i>Volver al Listado
                            </a>
                        }
                        else
                        {
                            <a asp-action="Calendarios" class="btn btn-outline-secondary">
                                <i class="fas fa-arrow-left me-2"></i>Volver al Calendario
                            </a>
                        }

                        @if (rol != null && rol == "Administrador")
                        {
                            <div>
                                <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-warning me-2">
                                    <i class="fas fa-edit me-2"></i>Editar
                                </a>
                                <a href="#" class="btn btn-outline-primary">
                                    <i class="fas fa-users me-2"></i>Gestionar Participantes
                                </a>
                            </div>
                        }
                        else if (User?.Identity?.IsAuthenticated == true)
                        {
                            <a href="#" class="btn btn-success btn-lg">
                                <i class="fas fa-user-plus me-2"></i>Inscribirse en esta Carrera
                            </a>
                        }
                    </div>
                </div>
            </div>

            <!-- Sección de Participantes (Opcional) -->
            @if (Model?.Registros?.Any() == true)
            {
                <div class="row mt-5">
                    <div class="col-12">
                        <div class="card border-0 shadow-sm">
                            <div class="card-header bg-light">
                                <h4 class="card-title mb-0">
                                    <i class="fas fa-list-ol text-primary me-2"></i>
                                    Participantes Registrados
                                </h4>
                            </div>
                            <div class="card-body">
                                <table class="table table-striped align-middle text-center">
                                    <thead class="table-dark">
                                        <tr>
                                            <th>Corredor</th>
                                            <th>Posición</th>
                                            <th>Kilómetro</th>
                                            <th>Último avance</th>
                                        </tr>
                                    </thead>
                                  
                                </table>
                                @if (Model.Registros.Count > 5)
                                {
                                    <div class="text-center mt-3">
                                        <a href="#" class="btn btn-outline-secondary">Ver todos los participantes (@Model.Registros.Count)</a>
                                    </div>
                                }
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
   
</div>

<table class="table table-striped align-middle text-center">
    <thead class="table-dark">
        <tr>
            <th>Corredor</th>
 
            <th>Acciones</th>
        </tr>
    </thead>

    <tbody id="tabla-corredores">
        @foreach (var registro in Model.Registros)
        {
            <tr id="row-@registro.Corredor.Id">
                <td>@registro.Corredor.NombreCompleto</td>

                <td>
                    <a asp-action="SeguimientoDeCorredor" asp-route-id="@registro.Id" class="btn btn-sm btn-info">Seguir corredor</a>
                </td>
            </tr>
        }
    </tbody>

</table>



<style>
    .card {
        transition: transform 0.2s ease, box-shadow 0.2s ease;
    }

        .card:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15) !important;
        }

    .bg-primary, .bg-success, .bg-info, .bg-warning {
        background: linear-gradient(135deg, var(--bs-primary) 0%, var(--bs-primary) 100%) !important;
    }

    .bg-success {
        background: linear-gradient(135deg, var(--bs-success) 0%, var(--bs-success) 100%) !important;
    }

    .bg-info {
        background: linear-gradient(135deg, var(--bs-info) 0%, var(--bs-info) 100%) !important;
    }

    .bg-warning {
        background: linear-gradient(135deg, var(--bs-warning) 0%, var(--bs-warning) 100%) !important;
    }

    .fas {
        font-family: 'Font Awesome 5 Free';
        font-weight: 900;
    }

    /* Fallbacks para iconos */
    .fa-edit::before {
        content: "✏️";
    }

    .fa-arrow-left::before {
        content: "⬅️";
    }

    .fa-map-marker-alt::before {
        content: "📍";
    }

    .fa-flag-checkered::before {
        content: "🏁";
    }

    .fa-route::before {
        content: "🛣️";
    }

    .fa-running::before {
        content: "🏃";
    }

    .fa-info-circle::before {
        content: "ℹ️";
    }

    .fa-calendar-alt::before {
        content: "📅";
    }

    .fa-users::before {
        content: "👥";
    }

    .fa-user-plus::before {
        content: "➕";
    }

    .fa-list-ol::before {
        content: "📋";
    }
</style>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

<script>
    window.carreraId = @Model.Id;
</script>

<!-- Incluir el script adaptado de SignalR debajo -->
<!--
<script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/8.0.0/signalr.min.js"></script>

<script>
    const connection = new signalR.HubConnectionBuilder()
        .withUrl("https://localhost:7247/carrerasHub")
        .build();

    // Evento recibido desde el servidor
    connection.on("CarreraActualizada", (data) => {
        console.log("CarreraActualizada:", data);

        // data = { corredorId, posicion, kilometro, hora }

        const fila = document.querySelector(`#row-${data.corredorId}`);

        if (fila) {
            // Actualizar valores existentes
            fila.querySelector(".posicion").textContent = data.posicion;
            fila.querySelector(".kilometro").textContent = data.kilometro;
            fila.querySelector(".tiempo").textContent =
                data.hora ? new Date(data.hora).toLocaleTimeString() : "—";
        }
        else {
            // Crear nueva fila si no existe
            const nuevaFila = `
                <tr id="row-${data.corredorId}">
                    <td>${data.nombre ?? "Corredor"}</td>
                    <td class="posicion">${data.posicion}</td>
                    <td class="kilometro">${data.kilometro}</td>
                    <td class="tiempo">${new Date(data.hora).toLocaleTimeString()}</td>
                </tr>
            `;
            document.querySelector("#tabla-corredores").insertAdjacentHTML("beforeend", nuevaFila);
        }
    });

    // Conectarse y unirse al grupo correspondiente
    connection.start().then(() => {
        connection.invoke("UnirseACarrera", @Model.Id);
    }).catch(err => console.error(err));
</script>
-->
