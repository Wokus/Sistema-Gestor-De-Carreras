@using System.Threading;
@using System.Security.Claims;
@{
    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();
}

@{
    var userIdString = User.FindFirstValue(ClaimTypes.NameIdentifier);
    int userId = 0;
    int.TryParse(userIdString, out userId);

    var email = User.FindFirstValue(ClaimTypes.Email);
    var nombre = User.FindFirstValue(ClaimTypes.Name);
    var rol = User.FindFirstValue(ClaimTypes.Role);
}

@if (rol != null && rol == "Administrador")
{

        <ul class="navbar-nav flex-grow-1">
            <li class="nav-item">

            <a class="nav-link text-dark" asp-controller="Corredors" asp-action="Index" asp-route-id="@userId">Corredores</a>
                </li>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-controller="Carreras" asp-action="Index" asp-route-id="@userId">Carreras</a>
            </li>
            <li class="nav-item">
                <a class="nav-link text-dark" asp-controller="Carreras" asp-action="CalendariosAdmin"> Calendario(Admin)</a>
            </li>
        </ul>
      
}
else
{
    
}
